apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-switch-service.fullname" . }}-test-connection"
  labels:
    {{- include "auth-switch-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-switch-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
