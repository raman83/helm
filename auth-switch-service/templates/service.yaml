apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-switch-service.fullname" . }}
  labels:
    {{- include "auth-switch-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auth-switch-service.selectorLabels" . | nindent 4 }}
